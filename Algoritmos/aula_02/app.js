// 1 - Escreva um algoritmo que receba um número e retorne o número com 100%.
/* let inputNumber: number = Number(prompt('Informe valor para calculo de aumento: '));

if (inputNumber) {
  inputNumber += inputNumber;
  console.log(inputNumber);
} */
// 2 - Escreva um algoritmo que receba o valor de um produto e retorne o produto mais um aumento de 50%.
/* const inputPriceProduct: number = Number(prompt('Informe o valor do produto: '));

if (inputPriceProduct) {
  let priceIncrease: number = inputPriceProduct * 0.5;
  let priceFinal: number = inputPriceProduct + priceIncrease;

  console.log(`Valor original do produto: ${inputPriceProduct}`);
  console.log(`Valor do aumento do produto: ${priceIncrease} `);
  console.log(`Valor final do produto:  ${priceFinal}`);
} */
// 3 - Escreva um algoritmo que receba o valor que será investido e mais o aumento de 30% de rentabilidade.
/* const inputMoneyInvested: number = Number(prompt('Digite o valor para ser investido: '));
if (inputMoneyInvested) {
  let moneyIncrease: number = inputMoneyInvested * 0.3;
  let moneyFinal: number = moneyIncrease + inputMoneyInvested;

  console.log(`Valor do investimento é de : ${inputMoneyInvested}`);
  console.log(`Valor do retorno do investimento é de  ${moneyIncrease} `);
  console.log(`Valor final com o retorno de investimento é de :  ${moneyFinal}`);
} */
// 4 - Escreva um algoritmo que recebe o valor do salario e retorne com um aumento de 15%.
/* const inputSalary: number = Number(prompt('Informe valor do salário para reajuste: '));

if (inputSalary) {
  let salaryIncrease: number = inputSalary * 0.15;
  let salaryFinal: number = inputSalary + salaryIncrease;

  console.log(`O valor original do salário  é : ${inputSalary}`);
  console.log(`O valor do aumento do salário é: ${salaryIncrease}`);
  console.log(`O valor final do salário é: ${salaryFinal}`);
} */
// 5 - Escreva um algoritmo que recebe o valor de um produto e retorne com um desconto de 7%.
/* const inputPriceProduct: number = Number(prompt('Informe o valor do produto: '));

if (inputPriceProduct) {
  let priceIncrease: number = inputPriceProduct * 0.07;
  let priceFinal: number = inputPriceProduct - priceIncrease;

  console.log(`Valor original do produto: ${inputPriceProduct}`);
  console.log(`Valor do desconto do produto: ${priceIncrease.toFixed(2)} `);
  console.log(`Valor final do produto:  ${priceFinal}`);
} */
// 6 - Escreva um algoritmo que recebe um salario e retorna o valor com descontos de: 6% - transporte; 8 - Inss.
/* const inputSalary: number = Number(prompt('Informe valor do salário para calculo de impostos: '));

if (inputSalary) {
  let inssDiscount: number = inputSalary * 0.6;
  let transportDiscount: number = inputSalary * 0.08;
  let discountFinal: number = inssDiscount + transportDiscount;
  let salaryFinal: number = inputSalary - discountFinal;

  console.log(`O valor original do salário  é : ${inputSalary}`);
  console.log(`O valor do desconto do vale transporte é: ${transportDiscount}`);
  console.log(`O valor do desconto do INSS é: ${inssDiscount}`);
  console.log(`O valor total dos descontos são:  ${discountFinal}`);
  console.log(`O valor do salário liquido é de: ${salaryFinal}`);
} */
