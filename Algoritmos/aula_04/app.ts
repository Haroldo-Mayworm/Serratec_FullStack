// 1 - Escreva um loop que retorne os números de 10 -> 0.
/* for (let i = 10; i >= 0; i--) {
  console.log(`Número - ${i}`);
} */

// 2 - Escreva um loop que retorne a tabuada de 5.
/* let i = 1;

while (i <= 10) {
  let result = `${i} x 5 = ${i * 5}`;
  console.log(result);
  i++;
} */

// 3 - Escreva um loop que recebe um número e retorna do número recebido até 0.
/* let inputNumber: number = Number(prompt('Digite um número: '));

while (inputNumber >= 0) {
  console.log(inputNumber);
  inputNumber--;
} */

// 4 - Escreva um loop que retorne a tabuada de acordo com o número que for recebido.
/* let inputData: number = Number(prompt('Digite um número: '));
let i: number = 1;

while (i <= 10) {
  let result = `${i} x ${inputData} = ${i * entradaDado}`;
  console.log(result);
  i++;
} */